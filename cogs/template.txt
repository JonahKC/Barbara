from discord.ext import commands

class NameOfCog(commands.Cog):
  def __init__(self, bot):
    self.bot = bot
  
  
  
def setup(bot):
  bot.add_cog(NameOfCog(bot))

IN ORDER TO MAKE A COMMAND NOT TRY TO EXECUTE WHEN NOT PASSED A SUBCOMMAND:
invoke_without_subcommand=False in @commands.command